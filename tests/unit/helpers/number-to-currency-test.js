import {
  numberToCurrency
} from 'good-tipper/helpers/number-to-currency';

module('NumberToCurrencyHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = numberToCurrency(42);
  ok(result);
});
